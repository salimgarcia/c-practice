/*Copy input to output and replace each tab by \t , each backspace by \b, and each backslash by \\*/

#include <stdio.h>

int main() {
    int c;

    while ((c = getchar()) != EOF) {

        if (c == '\t') {
            
        }

        putchar(c);
    }
}